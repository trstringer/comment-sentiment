apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "comment-sentiment.fullname" . }}-test-connection"
  labels:
    {{- include "comment-sentiment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "comment-sentiment.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
